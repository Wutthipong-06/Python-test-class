num = [4, 2, 9, 1, 5, 6]

length = len(num)
print(f"Length of the list: {length}")

total_sum = sum(num)
print(f"SUm of all elements: {total_sum}")

max_value = max(num)
print(f"Maximun value: {max_value}")

mix_vale = min(num)
print(f"minimu value: {max_value}")

sorted_num = sorted(num)
print(f"Sorted list: {sorted_num}")

bool_list = [False, True, False]
any_true = any(bool_list)
print(f"Is any element True? {any_true}")

all_true = all(bool_list)
print(f"Are all elements True? {all_true}")

string = "hello"
char_list = list(string)
print(f"List of characters: {char_list}")

reversed_num = list(reversed(num))
print(f"Rever list: {reversed_num}")

enumerate_num = list(enumerate(num))
print(f"Enumerate list: {enumerate_num}")